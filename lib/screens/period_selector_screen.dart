// This is a placeholder file for the period selector screen
// The functionality is already incorporated in the home screen
import 'package:flutter/material.dart';

class PeriodSelectorScreen extends StatelessWidget {
  const PeriodSelectorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Period Selector Screen'),
      ),
    );
  }
}