name: wealth_comes
description: "财富累积器 - 一个帮助用户直观看到工资如何随时间累计增长的应用"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  flutter_local_notifications: ^17.2.2
  workmanager: ^0.5.1
  fl_chart: ^0.68.0
  google_fonts: ^6.1.0
  intl: ^0.19.0
  path_provider: ^2.1.1
  share_plus: ^7.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
